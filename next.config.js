/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    NEXT_PUBLIC_APP_NAME: 'EduWeave',
    NEXT_PUBLIC_APP_VERSION: '2.0.0',
  }
};

module.exports = nextConfig;
